# Agile Business — Environment Variables
# Скопируйте в .env и заполните реальными значениями.
# ВСЕ переменные с пометкой (ОБЯЗАТЕЛЬНО) должны быть заданы.

# Application
DEBUG=false
# (ОБЯЗАТЕЛЬНО) python -c "import secrets; print(secrets.token_urlsafe(48))"
SECRET_KEY=
DOMAIN=example.com

# Database
# (ОБЯЗАТЕЛЬНО) Пароль PostgreSQL (используется в docker-compose)
DB_PASSWORD=
# Строка подключения конструируется автоматически в docker-compose

# Redis
# (ОБЯЗАТЕЛЬНО) Пароль Redis
REDIS_PASSWORD=

# S3 / MinIO
# (ОБЯЗАТЕЛЬНО)
MINIO_USER=
MINIO_PASSWORD=

# Elasticsearch
ELASTIC_PASSWORD=changeme

# CORS — JSON-массив фронтенда (для Vite часто нужны 5173 и 5174)
# При DEBUG=false включён CSRF: origin должен быть в этом списке (или localhost/127.0.0.1 — см. csrf.py)
CORS_ORIGINS=["http://localhost:5173","http://localhost:5174","http://127.0.0.1:5173","http://127.0.0.1:5174"]

# Email (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@example.com

# Telegram Bot (опционально)
TELEGRAM_BOT_TOKEN=
TELEGRAM_ADMIN_CHAT_ID=

# Spotify API (опционально)
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=

# Sentry (опционально)
SENTRY_DSN=

# Admin seed — задайте при первом запуске, потом уберите
ADMIN_SEED_EMAIL=admin@example.com
ADMIN_SEED_PASSWORD=

# Uvicorn workers
WORKERS=2

# Порт фронтенда
FRONTEND_PORT=3000
